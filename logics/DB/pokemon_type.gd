
extends Node

export(String) var name = ""
export(int) var id = 0
export(Array) var ineffective = [] # the types this type is ineffective against.
export(Array) var no_effect = [] # the types this type has no effect against.
export(Array) var resistance = [] # the types this type is resistant to.
export(Array) var super_effective = [] # the types this type is super effective against.
export(Array) var weakness = [] # the types this type is weak to.
